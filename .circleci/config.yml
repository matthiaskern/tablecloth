version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.5

    working_directory: ~/repo

    steps:
      - checkout

      - run: npm install -g esy

      # install dependencies
      - run: esy
